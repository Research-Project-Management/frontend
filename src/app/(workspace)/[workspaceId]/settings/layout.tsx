import SettingLayout from "@/features/workspaces/settings/components/layout/SettingLayout";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <SettingLayout>{children}</SettingLayout>;
}
