import LibraryLayout from "@/features/library/components/layout/LibraryLayout";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <LibraryLayout>{children}</LibraryLayout>;
}
